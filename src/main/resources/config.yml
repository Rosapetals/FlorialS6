mongo:
  uri:
  database:

discord:
  token:
  serverid:
  verificationChannel:
  trustedStaffId:
  staffId:
  adminChannel:
  chatbotChannel:
  openaiToken:
  shiftChannel:
  awaitingVerificationRole:
  verifiedRole:
  chatlogChannel:
  commandlogChannel:

general:
  staffChatFormat: "" # Use minimessage formatting, %player% will get the player, %message% will get the message
